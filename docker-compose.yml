version: "3.9"

services:
  api:
    build: .
    environment:
      - DOWNLOAD_DIR=/downloads
      - CONFIG_DIR=/config
      - TZ=UTC
    volumes:
      - /mnt/data/data/music/Spotify Playlists:/downloads
      - ./data/config:/config
    ports:
      - "8082:8080"

  ui:
    build: ./app/ui
    environment:
      - VITE_API_BASE=/api
    ports:
      - "8083:80"
    depends_on:
      - api
